/**
 * Application layer — use-cases and port definitions.
 *
 * <p>Contains inbound ports (driving interfaces invoked by infrastructure adapters)
 * and outbound ports (driven interfaces implemented by infrastructure adapters).
 * This layer orchestrates domain logic but delegates all I/O to ports.</p>
 */
package ai.fatwa.gateway.application;
